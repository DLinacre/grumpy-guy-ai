/**
 * Legacy component path — preserved for backwards compatibility.
 * `AuthPanel` keeps its original props: `{ onClose }`.
 */
export { AuthDialog as AuthPanel } from '../features/auth/ui/auth-dialog';
export type { AuthDialogProps as AuthPanelProps } from '../features/auth/ui/auth-dialog';

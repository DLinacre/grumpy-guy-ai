/**
 * Legacy component path — preserved for backwards compatibility.
 * `Settings` keeps its original props: `{ preferences, onChange, onClear }`.
 */
export { SettingsPanel as Settings } from '../features/preferences/ui/settings-panel';
export type { SettingsPanelProps as SettingsProps } from '../features/preferences/ui/settings-panel';

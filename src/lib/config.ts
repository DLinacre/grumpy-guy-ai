/**
 * Legacy config entry point — preserved for backwards compatibility.
 * Implementation moved to `src/shared/config/env.ts` (now runtime-validated).
 */
export { config, supabaseConfigured } from '../shared/config/env';
export type { AppConfig } from '../shared/config/env';
